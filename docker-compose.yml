version: '3.8'

services:
  devops-agent:
    build: .
    container_name: devops-agent-live
    stdin_open: true
    tty: true
    restart: always
    stdin_open: true
    tty: true
    env_file:
      - .env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock